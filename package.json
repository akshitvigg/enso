{
  "name": "draw-app",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=apps/http-backend",
    "dev": "turbo run dev --filter=apps/http-backend",
    "start": "turbo run start --filter=apps/http-backend",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
